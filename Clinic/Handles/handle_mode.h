#ifndef HANDLE_MODE_H
#define HANDLE_MODE_H
#include <QComboBox>

class Handle_mode
{
public:
    Handle_mode();
    void fillFilterBySpeciality(QComboBox* combo);
};

#endif // HANDLE_MODE_H
